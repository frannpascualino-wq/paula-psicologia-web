import { doctorData } from '../data/doctorData';

// Optional theme entrypoint for future multi-doctor theming.
// Current CSS already uses static tokens; this export keeps color settings centralized.
export const theme = doctorData.theme;
